# A non-repeating set of 9 cells; general term for row, column and nonet.
class House
  @cells = []

  def <<(cell)
    # Add cell to array of cells
    # Check to make sure we aren't adding past 9th cell
    # Check to make sure the class is actually a cell.
    # Check for validity here to make sure we aren't adding repeat cell?
  end
end
